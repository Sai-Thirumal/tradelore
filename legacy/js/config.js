// Base URL of the Python API.
// In dev: http://localhost:8000  (uvicorn runs here)
// In prod: change to your deployed API URL
const API_URL = '';   // empty string = same origin (works when FastAPI serves the frontend)
